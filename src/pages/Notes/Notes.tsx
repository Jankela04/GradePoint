import Navbar from "../../components/Navbar/Navbar";

type Props = {};

const Notes = (props: Props) => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default Notes;
